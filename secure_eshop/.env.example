# Example environment variables file
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit the actual .env file to version control!

# Django settings
SECRET_KEY=your-django-secret-key-goes-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# Database settings (if using external database)
# DB_NAME=your_db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=your_db_host
# DB_PORT=your_db_port

# Email settings
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
ADMIN_EMAIL=admin@example.com

# Field-level encryption key 
# Generate with: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())
FIELD_ENCRYPTION_KEY=generate-your-encryption-key-here

# SSL/TLS Configuration for MySQL (uncomment and set if using MySQL with SSL)
# DB_SSL_CA=/path/to/ca-cert.pem
# DB_SSL_CERT=/path/to/client-cert.pem  # Optional if client certificates required
# DB_SSL_KEY=/path/to/client-key.pem    # Optional if client certificates required

# SSL/TLS Configuration for PostgreSQL (uncomment and set if using PostgreSQL with SSL)
# DB_SSL_MODE=require  # Options: disable, allow, prefer, require, verify-ca, verify-full
# DB_SSL_CERT=/path/to/client-cert.pem  # Optional
# DB_SSL_KEY=/path/to/client-key.pem    # Optional  
# DB_SSL_CA=/path/to/ca-cert.pem        # Optional for verify-ca or verify-full modes